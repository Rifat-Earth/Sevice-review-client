import React, { useEffect } from 'react';

const AddService = () => {
    useEffect(() => {
            document.title = "AddService | Service-review";
          }, []);
    return (
        <div>
           addservice 
        </div>
    );
};

export default AddService;